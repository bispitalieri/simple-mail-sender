---
name: Configurable (`**/config.yaml` etc.)
about: Request support for a new configuration option
title: "[config]: "
labels: ["config", "enhancement"]
assignees: []
---

## Field proposal

- YAML path (e.g. `smtp.tls.ciphersuites`):
- Type (string / int / bool / list / map):
- Default value:
- Description:

## Use case

Why is this needed? Which SMTP server, which deployment scenario?

## Backwards compatibility

Does this change the meaning of existing fields, or is it purely additive?